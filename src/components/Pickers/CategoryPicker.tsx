import { ICategory } from '@/types/app'
import React from 'react'

interface ICategoryPickerProps {
    category: ICategory
    onSelect: (category: ICategory) => void
}

const CategoryPicker = (props: ICategoryPickerProps) => {
    return (
        <div />
    )
}

export default CategoryPicker
